# scribe server 直接相依(不含 docker 內的 vLLM/qwen-asr —— 那些在 docker/ 自成一套)
# 安裝:pip install -r requirements.txt
# 註:torch/torchaudio 請依你的 CUDA 版本安裝對應 wheel;funasr 會用到。

# Web / API
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32       # 檔案上傳 (UploadFile/Form)
websockets==16.1.1
httpx==0.28.1
openai==2.46.0                 # 打 vLLM/Ollama 的 OpenAI 相容端點

# ASR / 音訊
funasr==1.3.22                 # 預覽 paraformer-streaming + fsmn-vad + CAM++ 語者
torch==2.9.1
torchaudio==2.9.1
numpy==2.2.6
soundfile==0.14.0
librosa==0.11.0                # 上傳檔案載入/重採樣
OpenCC==1.4.1                  # 簡->繁台灣用語

# 儲存 / RAG
aiosqlite==0.22.1
sqlite-vec==0.1.9              # SQLite 向量檢索 (⑥ RAG)
